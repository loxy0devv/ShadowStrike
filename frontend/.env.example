# ShadowStrike Phantom Dashboard - Environment Variables

# API base URL (default: https://localhost:9443)
# Only change this if the EDR service runs on a different host/port
VITE_API_URL=https://localhost:9443
